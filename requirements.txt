# --- Orchestration ---
langgraph>=0.6.0
langchain>=0.3.0
langchain-core>=0.3.0
langchain-google-genai>=2.0.0      # Gemini chat + embeddings
langchain-groq>=0.2.0              # Groq fallback LLM
langchain-openai>=0.2.0            # optional: swap-in provider
langchain-anthropic>=0.3.0         # optional: swap-in provider
langgraph-checkpoint-postgres>=2.0.0

# --- Vector DB / retrieval ---
qdrant-client>=1.12.0
rank-bm25>=0.2.2                   # sparse/BM25 scoring
sentence-transformers>=3.2.0       # BGE reranker (self-hosted, free)

# --- API layer ---
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
sse-starlette>=2.1.0               # streaming responses
python-dotenv>=1.0.1
pydantic>=2.9.0
pydantic-settings>=2.6.0

# --- Ingestion ---
httpx>=0.27.0
beautifulsoup4>=4.12.3
markdownify>=0.13.1
tenacity>=9.0.0                    # retries / backoff

# --- Evaluation & observability ---
ragas>=0.2.6
datasets>=3.0.0
langsmith>=0.1.140

# --- Tool integration ---
PyGithub>=2.4.0

# --- Persistence ---
psycopg[binary]>=3.2.0
